{
  "name": "automate",
  "private": true,
  "packageManager": "bun@1.3.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,landing,app,worker -c blue,green,magenta,yellow \"bun run --filter @automate/api dev\" \"bun run --filter @automate/landing dev\" \"bun run --filter @automate/app dev\" \"bun run --filter @automate/worker dev\"",
    "dev:api": "bun run --filter @automate/api dev",
    "dev:landing": "bun run --filter @automate/landing dev",
    "dev:app": "bun run --filter @automate/app dev",
    "dev:worker": "bun run --filter @automate/worker dev",
    "build": "bun run --filter @automate/core build && bun run --filter @automate/ui build && bun run --filter @automate/api build && bun run --filter @automate/landing build && bun run --filter @automate/app build && bun run --filter @automate/worker build",
    "db:migrate": "bun run --filter @automate/api migrate",
    "db:seed": "bun run --filter @automate/api seed",
    "lint": "biome check .",
    "lint:fix": "biome check --fix .",
    "format": "biome format --write .",
    "typecheck": "tsc -b",
    "test": "bun run --filter @automate/core test"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/node": "^22.0.0",
    "concurrently": "^9.0.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {}
}
